cmake_minimum_required(VERSION 4.0)

project(LibMathF)

include_directories(${CMAKE_SOURCE_DIR}/include)
aux_source_directory(${CMAKE_SOURCE_DIR}/src SRC)
set(LIBRARY_OUTPUT_PATH ${CMAKE_SOURCE_DIR}/out)

add_library(mathf STATIC ${SRC})